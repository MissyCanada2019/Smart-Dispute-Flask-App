{
  "build": {
    "nixpacks": true
  },
  "deploy": {
    "startCommand": "python manage.py init-db --env=production && gunicorn --bind 0.0.0.0:$PORT --workers 4 --threads 2 --timeout 120 wsgi:app",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}